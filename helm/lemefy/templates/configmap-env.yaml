kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "lemefy.fullname" $ }}-configenv
data:
  {{- $configEnv := default dict .Values.lemefy.configEnv }}
  {{- $adminPanelUrl := .Values.lemefy.adminPanelUrl }}
  {{- $configAdminPanelUrl := dig "configEnv" "ADMIN_PANEL_URL" "" .Values.lemefy }}
  {{- if (index .Values "lemefy-rag-api" "enabled") }}
  RAG_API_URL: http://{{ include "rag.fullname" (index .Subcharts "lemefy-rag-api") | lower }}.{{ .Release.Namespace | lower }}.svc.cluster.local:8000 
  {{- end }}
  {{- if and (not (dig "configEnv" "MEILI_HOST" "" .Values.lemefy)) .Values.meilisearch.enabled }}
  MEILI_HOST: http://{{ include "meilisearch.fullname" .Subcharts.meilisearch }}.{{ .Release.Namespace | lower }}.svc.cluster.local:7700
  {{- end }}
  {{- if and (not (dig "configEnv" "MONGO_URI" "" .Values.lemefy)) .Values.mongodb.enabled }}
  MONGO_URI: mongodb://{{ include "mongodb.service.nameOverride" .Subcharts.mongodb }}.{{ .Release.Namespace | lower }}.svc.cluster.local:27017/Lemefy
  {{- end }}
  {{- if and (not (dig "configEnv" "USE_REDIS" "" .Values.lemefy)) .Values.redis.enabled }}
  USE_REDIS: "true"
  {{- end }}
  {{- if and (not (dig "configEnv" "REDIS_URI" "" .Values.lemefy)) .Values.redis.enabled }}
  REDIS_URI: {{ include "lemefy.bundledRedisURI" . }}
  {{- end }}
  {{- if and $adminPanelUrl (not $configAdminPanelUrl) }}
  ADMIN_PANEL_URL: {{ $adminPanelUrl | quote }}
  {{- end }}
  {{- if and .Values.langfuseFanout.enabled (not (hasKey $configEnv "LANGFUSE_FANOUT_ENABLED")) }}
  LANGFUSE_FANOUT_ENABLED: "true"
  {{- end }}
  {{- if and .Values.langfuseFanout.enabled (not (hasKey $configEnv "LANGFUSE_FANOUT_COLLECTOR_URL")) }}
  LANGFUSE_FANOUT_COLLECTOR_URL: http://{{ include "lemefy.langfuseFanout.fullname" . }}.{{ .Release.Namespace | lower }}.svc.cluster.local:{{ .Values.langfuseFanout.service.port }}
  {{- end }}
  {{- if and .Values.langfuseFanout.enabled (not (hasKey $configEnv "LANGFUSE_FANOUT_TENANT_DESTINATIONS")) }}
  {{- include "lemefy.langfuseFanout.validateDestinationKeys" . }}
  {{- $tenantDestinations := list }}
  {{- range $name, $destination := .Values.langfuseFanout.tenant.destinations }}
  {{- $tenantDestinations = append $tenantDestinations (printf "%s=%s" $name $destination.baseUrl) }}
  {{- end }}
  LANGFUSE_FANOUT_TENANT_DESTINATIONS: {{ join "," $tenantDestinations | quote }}
  {{- end }}
  {{- if $configEnv }}
  {{- $renderedConfigEnv := $configEnv }}
  {{- if and $adminPanelUrl (hasKey $configEnv "ADMIN_PANEL_URL") (not $configAdminPanelUrl) }}
  {{- $renderedConfigEnv = omit $configEnv "ADMIN_PANEL_URL" }}
  {{- end }}
  {{- if $renderedConfigEnv }}
  {{- toYaml $renderedConfigEnv | nindent 2 }}
  {{- end }}
  {{- end }}
