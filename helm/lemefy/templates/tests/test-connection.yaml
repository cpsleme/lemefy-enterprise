apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lemefy.fullname" . }}-test-connection"
  labels:
    {{- include "lemefy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lemefy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
